# ResFoodKurir

Courier portal for ResFood. Built with Next.js App Router and Tailwind CSS.

## Setup
1. Create `.env.local` based on the provided credentials.
2. Install dependencies: `npm install`
3. Run development server: `npm run dev`

By default, this will run on http://localhost:3001.
